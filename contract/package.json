{
  "name": "hardhat-project",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:staking": "hardhat run scripts/deploySimpleStaking.js --network celo",
    "deploy:router": "hardhat run scripts/deployStCeloRouter.js --network celo",
    "deploy:yearn": "hardhat run scripts/deployYearnVault.js --network celo"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "hardhat": "^2.26.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.6.1"
  }
}
